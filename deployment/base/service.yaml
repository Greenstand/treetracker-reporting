apiVersion: v1
kind: Service
metadata:
  name: treetracker-reporting-api-ambassador-svc
  labels:
    app: treetracker-webmap-api
    name: treetracker-reporting-api-ambassador-svc
  namespace: webmap
spec:
  selector:
    app: tretracker-reporting-api
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: 3006
